# Local API (npm run dev in backend/) — not used by Docker; Docker reads docker/.env for the api service.
NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://to5:12345678@127.0.0.1:5432/to5delivery
JWT_SECRET=WaelProjectSecret_to5_delivery_jwt_hmac_v1
CORS_ORIGIN=http://localhost:5173

# Google Places (New) — server only; POST /api/places/search-text
GOOGLE_PLACES_API_KEY=

# ALLOW_REGISTER=false
